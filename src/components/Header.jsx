import React from "react";
import "./Header.css";

const Header = props => (
    <div>
        <ul className="nav header">
            <h2>Clicky Game</h2>
            <span>Score:</span>
        </ul>
    </div>
);

export default Header;