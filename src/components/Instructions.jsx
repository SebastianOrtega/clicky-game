import React from "react";
import "./Instructions.css";

const Instructions = props => (
  <div className="instructions">
    <h1 className="vertical-center">Don't click on any more than once!</h1>
  </div>
);

export default Instructions;
